type Client {
  id: Int
  name: String
  phone: String
  country: String
}

type Query {
  clients: [Client]
  client(id: Int): Client
}

type Mutation {
  addClient(name: String, phone: String, country: String): Client
}
